<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sun.dodofei.e560.m1126a">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ToolsClass.test_FlickrSQL"></activity>
        <activity android:name=".ListPage"></activity>
        <activity android:name=".Idolpiclist"></activity>
        <activity android:name=".Viewidol"></activity>
        <service android:name=".ToolsClass.SavetoFTP"></service>
        <service android:name=".ToolsClass.syncServer"></service>
        <service android:name=".ToolsClass.ftp_upjson_server"></service>
    </application>

</manifest>